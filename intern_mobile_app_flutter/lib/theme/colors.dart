import 'package:flutter/material.dart';

class AppColors {
  // ── Core Backgrounds ──
  static const Color background = Color(0xFF0a0a0f);
  static const Color card = Color(0xFF111118);
  static const Color elevated = Color(0xFF191922);
  static const Color surface = Color(0xFF111118);

  // ── Accent — Green ──
  static const Color primary = Color(0xFF22d87a);
  static const Color primaryDark = Color(0xFF10b981);
  static const Color primaryMuted = Color(0x3322d87a);     // 20 % green for badges / chips
  static const Color secondary = Color(0xFF06e4f9);

  // ── Text ──
  static const Color text = Color(0xFFf0efe9);
  static const Color textSecondary = Color(0xFFa0a0b0);
  static const Color textMuted = Color(0x99f0efe9);
  static const Color textFaint = Color(0x4Df0efe9);

  // ── Status ──
  static const Color danger = Color(0xFFef4444);
  static const Color warning = Color(0xFFf59e0b);
  static const Color info = Color(0xFF3b82f6);
  static const Color success = Color(0xFF10b981);

  // ── Borders / Glass ──
  static const Color border = Color(0xFF1f1f2e);
  static const Color borderSubtle = Color(0x14ffffff);
  static const Color glassBg = Color(0xb30a0a0f);
  static const Color glassBorder = Color(0x1affffff);

  // ── Drawer ──
  static const Color drawerBg = Color(0xFF0d0d14);
  static const Color drawerItemActive = Color(0x1A22d87a);
  static const Color drawerDivider = Color(0xFF1a1a28);

  // ── Shimmer ──
  static const Color shimmerBase = Color(0xFF1a1a26);
  static const Color shimmerHighlight = Color(0xFF2a2a3a);

  // ── Bottom Nav ──
  static const Color navBarBg = Color(0xFF0d0d14);
  static const Color navUnselected = Color(0xFF555566);
}
